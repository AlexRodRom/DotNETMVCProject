
@model IEnumerable<DotNETMVCProject.Models.Product>

@{
    ViewBag.Title = "Products";
}


<div class="col-12 col-lg-2 leftSideMenu">
    <div>
        <Button class="btn btn-success">Edit Mode</Button>
    </div>
    @*<div *ngIf="productServ.editMode">
            <app-form></app-form>
        </div>*@

    @*<div>
            @using (Html.BeginForm("Product", "Home", FormMethod.Post))
            {
                <div class="form-group">
                    @Html.LabelFor(m => m.Id, "EAN")
                    @Html.TextBoxFor(m => m.Id, new { @class = "form-control" })
                </div>
                <div class="form-group">
                    <button class="tradeButton btn btn-success btn-block">Add Product</button>
                </div>

            }
        </div>*@
</div>
<div class="col-12 col-lg-12 ">

    @{ Html.RenderPartial("_ProductList"); }


</div>
<div class="col-lg-2 col-2 sideMenu">

    @{ Html.RenderPartial("_ProductCart"); } 

</div>





